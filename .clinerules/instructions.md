Before performing any task involving building, running or configuring the project, always read and summarize the following files first if they exist:

- .vscode\tasks.json

If any of these files are missing, continue without them.

Never proceed to implementation before reading relevant configuration files first.
