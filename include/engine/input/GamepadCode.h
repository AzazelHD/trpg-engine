#pragma once

// GamepadCode will mirror KeyCode but for controller buttons.
// Stub only — implement after keyboard input is working (Phase 11+).
//
// TODO: Define an enum class GamepadCode with button names (A, B, X, Y, Start, etc.).
// SDL_GameController provides the raw events you'll translate here.
enum class GamepadCode
{
    // TODO: add entries here
};
